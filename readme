mon_site/
├── .env
├── .gitignore
├── add_food.html
├── config.js
├── config.production.php
├── delete_food.php
├── get_best_score.php
├── get_foods.php
├── index.php
├── login.php
├── logout.php
├── save_score.php
├── script.js
├── style.css
├── vendor/
│   └── autoload.php
└── composer.json


Comment créer les tables en local dans phpmyadmin pour afficher le site (en local):

1.Schéma des tables :
#.Exécutez la requête suivante pour créer une table users :


CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL
);

CREATE TABLE foods (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    benefits TEXT,
    nutrients TEXT,
    user_id INT,
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE scores (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    score INT,
    FOREIGN KEY (user_id) REFERENCES users(id)
);


2.Utilisez les requêtes générées pour insérer les utilisateurs dans la base de données. 
Par exemple :

INSERT INTO users (username, password) VALUES ('user1', '$2y$10$examplehashforuser1');
INSERT INTO users (username, password) VALUES ('user2', '$2y$10$examplehashforuser2');
